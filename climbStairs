var climbStairs = function (n) {
    if (n < 2) {
        return 1;
    }
    return climbStairs(n - 1) + climbStairs(n - 2);
};

console.log(climbStairs(2)); //o/p -2
console.log(climbStairs(3)); //o/p -3
